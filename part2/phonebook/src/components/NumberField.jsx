const NumberField = ({number, handleNumber}) => <div>
                                            number <input value={number} onChange={handleNumber} />          
                                          </div>

export default NumberField